apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090    # Docker network mein Prometheus container ka naam
    isDefault: true                # Grafana mein default datasource hoga
    editable: true